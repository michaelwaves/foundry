activations_h5: ???
labels_csv: ???
positive_label: 1
negative_label: 0
hooks: []                # e.g. [block6, block8, block12]
normalize: unit          # unit | none

out_dir: /mnt/nw/home/m.yu/repos/foundry/outputs/steering/vectors/${now:%Y-%m-%d_%H-%M-%S}

hydra:
  run:
    dir: ${out_dir}
  output_subdir: null
  job:
    chdir: false
