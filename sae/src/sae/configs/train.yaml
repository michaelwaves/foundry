defaults:
  - _self_
  - trainer: matryoshka_batch_top_k

activations_path: /mnt/nw/home/m.yu/repos/foundry/tutorials/rfd3_na_tutorial/activations_new/activations.h5
hook_name: block6
architecture: matryoshka_batch_top_k

device: cuda:0
seed: 0

steps: 20000
batch_size: 4096
shuffle: true
log_every: 100

save_checkpoints: true
use_wandb: false
wandb_project: rfd3_sae

out_dir: /mnt/nw/home/m.yu/repos/foundry/outputs/train/${architecture}/${hook_name}/${now:%Y-%m-%d_%H-%M-%S}

hydra:
    run:
      dir: ${out_dir}
    output_subdir: null     # don't nest a .hydra/ folder inside out_dir
    job:                                                                                                                           
      chdir: false
                     