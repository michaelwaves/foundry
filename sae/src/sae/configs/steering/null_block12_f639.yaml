# @package steering
# Regression check: alpha=0 must reproduce a plain `saffron collect` run
# bit-for-bit. Catches accidental in-place edits in the steering hook closure.

block12:
  - mode: sae_feature
    sae_path: ${oc.env:FOUNDRY_ROOT,${hydra:runtime.cwd}}/outputs/sae/2026-04-26_15-38-55/train/block12/final.pt
    feature_id: 639
    alpha: 0.0
    apply_at_steps: all
