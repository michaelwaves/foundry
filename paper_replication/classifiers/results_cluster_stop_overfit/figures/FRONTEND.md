make a beautiful results gallery and visualizer in ~/repos/raftlanding, a nextjs project, at /blog/saeber
have a super brief intro, then the rest is beautiful graphs and figures
use recharts, have the data used to generate the 4 figures in /figures (but NOT the pngs themselves, use recharts) Fit the existing color style (3 main colors in globals.css, green, orange, blue)

For feature viz:
Have a sidebar to click through each feature number. Have filters near the top and maybe a simple searchbar. Display png images from the viz directory (just copy over to public folder)

Make sure everything is modern, sleek, playful, minimalist. Look at the existing styles and use inline tailwindcss for everything.

Do NOT write too much stuff, just a bit is ok. Use one random png to be a background image for artsy design.

Good luck, and have fun!